# Build artifacts
*.zip
*.tar.gz

# Vendor — installed via composer install; built into release zip via CI
vendor/

# OS / IDE
.DS_Store
.idea/
*.log
node_modules/

# Dev tools
.env
.env.*
ucp-for-woocommerce.zip
.phpunit.result.cache
.phpunit.cache/

# License config injected at download time — contains partner key
ucp-for-woocommerce-config.php
ucp-for-woocommerce-config.php
